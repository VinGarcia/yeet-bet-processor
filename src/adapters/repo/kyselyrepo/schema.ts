/**
 * Kysely database schema. No tables defined yet (slice 1); tables will be
 * added here as the data model grows.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface DB {}
